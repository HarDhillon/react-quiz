import { useFetchQuestionsQuery } from "../store";

function QuestionList({ options }) {
    // const { data, error, isFetching } = useFetchQuestionsQuery(options)


    return (
        <div>Question List</div>
    )
}

export default QuestionList